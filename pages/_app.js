import '../styles/globals.css'; // Adjust if your global Tailwind file is named differently or located elsewhere

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />;
}